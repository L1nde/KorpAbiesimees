<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ee.skyhigh.l1nde.korplaitused">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.Meetings" />
        <activity android:name=".activities.Members" />
        <activity android:name=".activities.AddMember" />
        <activity android:name=".activities.AddMeeting" />
        <activity android:name=".activities.NewMeeting" />
        <activity android:name=".activities.SingleMember"></activity>
    </application>

</manifest>